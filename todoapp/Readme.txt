1) Here we have create context folder with todoContext where we have created context and then action are stored in action.type.js and the reducer consists of code to make use of action and value passsed.

2) Components todoform where in all the form elements are present and todo component handles the logic to add and delete the form

3) This application is an example of react hooks to use reducers so that we can modify state that is add and remove states using context API.